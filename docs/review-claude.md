# Claude 复盘（只读审查，产品代码已另改）

## Bug

### P0
- 「本周期 Token」表原先吃自然周 `by_model_periods`，副标题还写周一 00:00。官方窗已经在后端的 `cycle_by_model`，前端没读。
- Claude 不可用兜底把开发调试话术露给用户。

### P1
- Codex/Claude 周期数为 0 时被 UI 藏掉，看起来像没做。Claude 本地库里本来就没有会话。
- 单位设置只管表格/状态条，顶部六宫格还走 `Intl` 紧凑格式。
- `useDisplayPrefs` 页面和状态条各一份 React state，改配置底栏不刷新。
- 临近重置时只看剩余小时，会把「周额度」误判成 5 小时窗。
- `auto` 以外的第三方 provider 被模型名误归一到 Codex。

### P2
- 重置剩余按小时四舍五入，临近边界会跳 1 小时。
- 核心前端契约缺少单测。

## 界面

1. 本周期表改成：已用 / 窗口类型 / 重置，跟底栏%同一口径。
2. 配置拆成三行：状态条、模型、单位分段按钮。
3. 默认不要单独的「自然周」芯片，周期数贴在各家%旁边。
4. 配置改完立刻同步底栏。
5. 无数据写「—」，有窗但 0 写「0」。
6. 不要科幻皮肤。

## 该删
- 把自然周冒充本周期的表头和列开关。
- 用户看不见的调试文案。
- 状态条上默认的重复「本周」合计。
